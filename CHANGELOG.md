# سجل التغييرات

يوثق هذا الملف التغييرات الجوهرية لكل إصدار من نواة النظام والإضافات المرفقة.

## النواة (Core)

### 1.0.0 — قيد الإعداد
- بناء النواة: التوجيه، الاتصال بقاعدة البيانات (PDO + بادئة جداول)، الجلسات، حماية CSRF، تسجيل الأخطاء.
- نظام صلاحيات وأدوار: مدير النظام، مدير المحتوى، مدير التواصل، مشاهد — قابلة للتعديل من لوحة التحكم.
- نظام تخصيص المصطلح الأساسي (عائلة/قبيلة/رابطة/ديوانية/جماعة/مخصص) مطبّق تلقائيًا في كل الواجهات.
- معالج تثبيت كامل من المتصفح: فحص المتطلبات، بيانات قاعدة البيانات مع اختبار الاتصال، إنشاء الجداول والبيانات الافتراضية تلقائيًا، إنشاء حساب المدير، إنشاء `config.php` بمفاتيح أمان عشوائية، قفل تلقائي بعد النجاح.
- لوحة تحكم عربية RTL: لوحة معلومات، إعدادات الهوية والمصطلحات والصفحة الرئيسية وجوال العائلة والتواصل والملفات وSEO والصيانة، إدارة المستخدمين الإداريين والأدوار، إدارة الإضافات (تثبيت/تفعيل/تعطيل/رفع ZIP/إزالة)، مكتبة وسائط مركزية، إدارة القوائم (رئيسية/جوال/تذييل)، إدارة المدن، إدارة الفروع الاختيارية (تُفعَّل تلقائيًا عند إضافة أول فرع)، سجل عمليات، نسخ احتياطي (قاعدة بيانات + ملفات) واستعادة آمنة.
- الصفحة الرئيسية العامة: تصميم Mobile First، Hero قابل للتخصيص، شريط أخبار متحرك، أقسام قابلة للترتيب والإظهار/الإخفاء من الإعدادات، إحصائيات عامة آمنة (عدد فقط، بدون أي بيانات شخصية).
- SEO أساسي: روابط صديقة، `sitemap.xml` و`robots.txt` ديناميكيان يحترمان مكان التركيب (جذر أو مجلد فرعي)، صفحة 404 مخصصة، بيانات Open Graph.
- تم اختبار: تثبيت كامل من المتصفح (جذر واستضافة فرعية)، تسجيل دخول لوحة التحكم، تثبيت/تفعيل/تعطيل/إعادة تفعيل إضافة دون تعطل النواة، إنشاء ونشر محتوى وعرضه علنيًا.

## الإضافات (Modules)

### pages (الصفحات الثابتة) — 1.0.0
- إدارة صفحات ثابتة (من نحن، سياسة الخصوصية، شروط الاستخدام، صفحات مخصصة) مع رابط مختصر (Slug) عربي تلقائي، حالة نشر، ترتيب عرض، إعدادات SEO، وإمكانية الإضافة لقائمة التذييل.

---

الإضافات التالية (الأخبار، المناسبات، رزنامة المناسبات، الجمعات، جوال العائلة، القائمة البريدية، شجرة النسب، المعرض، الأرشيف، الإعلانات) قيد التنفيذ حسب ترتيب الأولوية المحدد في متطلبات المشروع، وستُضاف كإدخالات مستقلة هنا فور اكتمال كل واحدة واختبارها.
