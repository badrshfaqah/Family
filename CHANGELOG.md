# سجل التغييرات

يوثق هذا الملف التغييرات الجوهرية لكل إصدار من نواة النظام والإضافات المرفقة.

## النواة (Core)

### 1.13.0
- مزامنة تلقائية «أول بأول» بعد أي تحديث للملفات (سحب من GitHub أو رفع يدوي): عند أول زيارة بعد وصول نسخة جديدة تُطبَّق ترحيلات قاعدة بيانات النواة، وتُرحَّل الإضافات المثبتة لإصداراتها الجديدة (مع تسجيل صلاحياتها المستجدة)، وتُثبَّت وتُفعَّل الإضافات المرفقة الجديدة تلقائيًا — دون أي تدخل يدوي. تعمل مرة واحدة فقط لكل تغيير فعلي (بصمة من إصدار النواة + إصدارات كل الإضافات)، وكل ما يُطبَّق يُسجَّل في سجل العمليات.
- بطاقة «مزامنة الإضافات» في شاشة تحديث النظام: تعرض ما ينتظر المزامنة (ترحيلات معلقة، إضافات جديدة، إضافات تغيّر إصدارها)، مع خيار لتعطيل/تفعيل التثبيت التلقائي للإضافات الجديدة وزر لتشغيل المزامنة فورًا.
- التحديث الذاتي من GitHub صار يشغّل المزامنة الكاملة نفسها بعد استبدال الملفات (بدل ترحيلات النواة فقط).

### 1.12.0
- إضافة جديدة مرفقة: **api (واجهة التطبيق)** — واجهة JSON عامة للقراءة تحت `/api/v1/…` يستهلكها تطبيق الجوال النيتف (iOS)، تعرض المحتوى المنشور العام فقط: هوية الموقع وألوانه، الرئيسية (مع الوفيات النشطة كعاجل)، الأخبار، الرزنامة، الجمعات، المعرض، الوفيات، الشعر، الأرشيف، شجرة النسب، الصفحات، والبحث. تحترم حالة كل إضافة (المعطّلة ترجع خطأً لطيفًا) ولا تعرض بيانات جوال العائلة أو القائمة البريدية إطلاقًا.
- نقطتا POST في نفس الواجهة لجوال العائلة (تسجيل رقم من التطبيق / طلب إلغاء اشتراك) بنفس حمايات النموذج العام: تقييد معدل 5 محاولات كل 15 دقيقة لكل IP، تحقق من صحة الرقم، اشتراط الموافقة الصريحة، منع التكرار برسالة عامة، وطلب الإلغاء يبقى مراجعةً يدوية من المدير.
- التوثيق الكامل للواجهة في `docs/api.md`.

### 1.2.0
- شاشة "بيانات تجريبية" صارت تنبّه بوضوح عندما تكون هناك إضافات غير مفعّلة (لن يُولَّد لها محتوى)، مع زر جديد يثبّت ويفعّل جميع الإضافات المرفقة دفعة واحدة قبل التوليد.
- إضافة رقم الإصدار لروابط ملفات CSS وJS في الموقع العام ولوحة التحكم وصفحات الدخول (`?v=1.2.0`)، بحيث يُجبر متصفح الزائر تلقائيًا على تحميل النسخة الجديدة من الملفات بعد كل تحديث بدل الاستمرار على النسخة المخزنة مؤقتًا.
- إصلاح حرج في `.htaccess` على خوادم Apache: مسارات لوحة التحكم (مثل `admin/login`) كانت تُوجَّه لنقطة دخول الموقع العام فتعطي 404، والآن تُوجَّه لنقطة دخول اللوحة الصحيحة. كما أصبح التوجيه لا يعتمد على `PATH_INFO` إطلاقًا ليعمل حتى على الاستضافات التي تعطّل `AcceptPathInfo`.
- إصلاح تخطيط لوحة التحكم: كانت القائمة الجانبية الثابتة تغطي على بداية المحتوى من اليمين (الهامش كان محجوزًا على الجهة اليسرى الخطأ في اتجاه RTL)، وإصلاح تمدد المحتوى أفقيًا خارج الشاشة على الجوال. تم التحقق بلقطات شاشة فعلية من متصفح على مقاسي سطح المكتب والجوال.
- توسعة شاملة لمولّد البيانات التجريبية: بدل عنصر واحد من كل نوع، صار يولّد محتوى غنيًا يملأ كل جزئية (6 صور متنوعة الألوان، 3 مدن، 3 صفحات، 6 أخبار بتصنيفين وتواريخ متدرجة مع مثبّت ومميز، 4 مناسبات قادمة وسابقة بأنواع مختلفة، 4 مواعيد رزنامة، 3 جمعات بأنماط تكرار مختلفة، شجرة نسب من 8 أفراد بثلاثة أجيال، 3 ألبومات بسنوات مختلفة فيها 12 صورة، تصنيفان و4 مواد أرشيفية، و3 إعلانات تغطي البطاقة والشريط العلوي والنافذة المنبثقة) — ليرى من يثبّت الموقع لأول مرة شكله الحقيقي مكتمل المحتوى، مع بقاء الحذف الكامل بضغطة واحدة.

### 1.1.0
- دعم تطبيق الويب التقدمي (PWA): بيان تطبيق (`manifest.webmanifest`) ديناميكي يعكس اسم الموقع ولونه الفعليين، أيقونات مولَّدة تلقائيًا من الشعار المرفوع (أو شعار افتراضي عند عدم وجود شعار) بأغراض any وmaskable منفصلة، Service Worker يخزّن الأصول الثابتة للعمل السريع دون اتصال ويترك لوحة التحكم ومعالج التثبيت دائمًا من الشبكة مباشرة، مع تحديث الأيقونات تلقائيًا عند تغيير الشعار أو ألوان الهوية.
- صفحة "Family" عامة (`/family`) على الموقع العام، متاحة للجميع دون تسجيل دخول أو أي صلاحية عبر رابطها المباشر (مرفقة مع البرنامج في كل نسخة): تعرّف بالنظام ومميزاته، وتسرد جميع الإضافات المكتشَفة على الخادم وحالتها (مفعّلة/غير مفعّلة) وسجل التحديثات كاملًا — كل ذلك مقروء مباشرة من ملفات الإضافات (`module.json`) وملف `CHANGELOG.md` عند كل زيارة، دون أي محتوى مكرَّر يدويًا، بحيث تتحدّث الصفحة تلقائيًا مع كل إصدار جديد.
- تذييل الموقع العام يتضمن رابط "تطوير المجرات" إلى almgrat.com.
- إضافة ملف `almgrat.json` في جذر المشروع (بيانات الرفع على منصة Almgrat Hub: الاسم والمعرّف ورقم الإصدار والملفات المستثناة والمحفوظة عند التحديث).
- إصلاح تنسيق عناوين سجل التحديثات في صفحة `/family`، وإزالة مفتاح إعدادات غير مستخدم من البذور الافتراضية.

### 1.0.0
- بناء النواة: التوجيه (مع ترتيب المسارات حسب درجة التحديد لمنع تعارض الإضافات)، الاتصال بقاعدة البيانات (PDO + بادئة جداول + مزامنة المنطقة الزمنية مع MySQL)، الجلسات، حماية CSRF، رمز تحقق مرئي بسيط، تحديد معدل الطلبات، تسجيل الأخطاء.
- نظام صلاحيات وأدوار: مدير النظام، مدير المحتوى، مدير التواصل، مشاهد — قابلة للتعديل من لوحة التحكم، وكل إضافة يمكن أن تسجّل صلاحياتها الخاصة.
- نظام تخصيص المصطلح الأساسي (عائلة/قبيلة/رابطة/ديوانية/جماعة/مخصص) مطبّق تلقائيًا في كل الواجهات.
- معالج تثبيت كامل من المتصفح: فحص المتطلبات، بيانات قاعدة البيانات مع اختبار الاتصال، إنشاء الجداول والبيانات الافتراضية تلقائيًا، إنشاء حساب المدير، إنشاء `config.php` بمفاتيح أمان عشوائية، قفل تلقائي بعد النجاح.
- لوحة تحكم عربية RTL: لوحة معلومات، إعدادات الهوية والمصطلحات والصفحة الرئيسية وجوال العائلة والتواصل والملفات وSEO والصيانة، إدارة المستخدمين الإداريين والأدوار، إدارة الإضافات (تثبيت/تفعيل/تعطيل/رفع ZIP/إزالة مع خيار حذف أو الاحتفاظ بالبيانات)، مكتبة وسائط مركزية، إدارة القوائم (رئيسية/جوال/تذييل)، إدارة المدن، إدارة الفروع الاختيارية (تُفعَّل تلقائيًا عند إضافة أول فرع) مع صفحات عامة اختيارية لكل فرع.
- الصفحة الرئيسية العامة: تصميم Mobile First، Hero قابل للتخصيص، شريط أخبار متحرك، بطاقة المناسبة القادمة مع عد تنازلي، قسم "قريبًا"، أقسام قابلة للترتيب والإظهار/الإخفاء من الإعدادات، إحصائيات عامة آمنة (عدد فقط، بدون أي بيانات شخصية).
- SEO: روابط صديقة، `sitemap.xml` و`robots.txt` ديناميكيان يحترمان مكان التركيب (جذر أو مجلد فرعي)، بحث عام لا يفهرس أي بيانات حساسة، صفحة 404 مخصصة، بيانات Open Graph.
- نسخ احتياطي بلغة PHP خالصة (قاعدة بيانات + ملفات) دون الاعتماد على أوامر خارجية، مع استعادة آمنة وأخذ نسخة تلقائية قبل أي استعادة.
- إعادة توجيه تلقائية (301) عند تغيير الرابط المختصر لأي محتوى (صفحة/خبر/مناسبة/أرشيف/ألبوم)، وبيانات Schema.org منظمة (NewsArticle وEvent) لصفحات الأخبار والمناسبات.
- شاشة "بيانات تجريبية" لتوليد وحذف نموذج تجريبي واضح لكل نوع محتوى بضغطة واحدة، دون أي أسماء أو أرقام حقيقية.
- إنشاء أدوار إدارية مخصصة جديدة من الواجهة (بجانب الأدوار الأربعة الأساسية)، ومسار كامل لاستعادة كلمة مرور المدير عبر بريد إلكتروني، بمرسل بريد داخلي بديل (SMTP اختياري أو mail() المدمجة) دون أي مكتبة خارجية.
- سحب وإفلات حقيقي لترتيب عناصر القوائم.
- أزرار مشاركة صريحة لـ X وتيليجرام بجانب واتساب على صفحات الأخبار والمناسبات والصفحات.
- تبويب "المظهر" الجديد: أنماط ألوان جاهزة قابلة للاختيار بضغطة، وخط قابل للاختيار، مطبّقان فعليًا على الموقع العام.
- ضغط الصور الأصلية المرفوعة فعليًا (تصغير الأبعاد وإعادة الترميز بجودة قابلة للتحكم)، وليس فقط الصورة المصغرة كما كان سابقًا.

## الإضافات (Modules)

### api (واجهة التطبيق) — 1.0.0
واجهة JSON عامة للقراءة فقط تحت `/api/v1` يستهلكها تطبيق الجوال — محتوى منشور عام فقط، مع نقطتي تسجيل/إلغاء اشتراك لجوال العائلة محميتين بتقييد المعدل. تعطيلها يوقف كل مساراتها فورًا دون أثر على بقية النظام.

### pages (الصفحات الثابتة) — 1.0.0
إدارة صفحات ثابتة (من نحن، سياسة الخصوصية، شروط الاستخدام، صفحات مخصصة) مع رابط مختصر عربي تلقائي، حالة نشر، ترتيب عرض، إعدادات SEO، وإمكانية الإضافة لقائمة التذييل.

### news (الأخبار) — 1.0.0
نظام أخبار كامل: تصنيفات، وسوم، صورة رئيسية وفيديو وروابط خارجية، اسم الكاتب، حالات نشر (مسودة/منشور/مجدول/مؤرشف)، تثبيت وتمييز، إعدادات SEO، قائمة عامة مقسّمة صفحات، صفحة خبر مفردة مع أخبار ذات صلة ومشاركة واتساب.

### events (المناسبات المنشورة) — 1.0.0
صفحات مناسبات كاملة (زواج/تخرج/حفل/وليمة/اجتماع/عزاء/تكريم/نوع مخصص): وصف ومحتوى كامل، صور ومعرض وفيديو ومرفقات، مدينة ومكان ورابط خرائط، تاريخ ووقت البداية والنهاية، وسوم، تثبيت وتمييز، إعدادات SEO. المناسبات السابقة لا تُحذف وتبقى قابلة للتعديل.

### calendar (رزنامة المناسبات) — 1.0.0
قائمة مواعيد قادمة سريعة ومستقلة عن صفحات المناسبات الكاملة، مع إمكانية ربط أي موعد بصفحة مناسبة موجودة أو تركه بدون صفحة. عرض بالشهر وعرض كقائمة قادمة، تصفية حسب النوع والمدينة، يدعم عدة مدن في آن واحد دون خريطة عامة مجمّعة.

### gatherings (الجمعات) — 1.0.0
جمعات بتكرار يومي/أسبوعي/شهري/أيام محددة/مخصص (وليس "أسبوعية" فقط)، مع نص وصفي محسوب تلقائيًا لعرض موعد التكرار بوضوح، وحالة نشطة/متوقفة مؤقتًا/منتهية.

### directory (جوال العائلة) — 1.0.0
قائمة تواصل خاصة بالكامل: لا صفحة عامة لعرض الأرقام أبدًا. نموذج تسجيل عام بموافقة صريحة ورمز تحقق ومنع تكرار، صفحة ذاتية لإدارة الاشتراك تُنشئ طلب مراجعة إداري بدلًا من حذف مباشر، إدارة إدارية كاملة (بحث/تصفية/كشف تكرار ودمج/استيراد وتصدير Excel)، وقوالب رسائل جاهزة (دون إرسال فعلي في هذا الإصدار).

### newsletter (القائمة البريدية) — 1.0.0
اشتراك بريدي مستقل عن جوال العائلة، إلغاء اشتراك ذاتي، تصنيف مشتركين، استيراد وتصدير Excel، بحث وتصفية — بدون أي عرض عام للبريد الإلكتروني.

### family-tree (شجرة النسب) — 1.0.0
تسلسل نسب بسيط غير محدود العمق (بدون بيانات شخصية حساسة)، عرض شجري وعرض تسلسلي مناسب للجوال، ربط اختياري بفرع.

### gallery (المعرض) — 1.0.0
ألبومات صور وفيديو مصنّفة حسب السنة والمدينة والفرع، مع إمكانية الربط بخبر أو مناسبة، رفع متعدد للصور، عرض شبكي متجاوب.

### archive (الأرشيف) — 1.0.0
أرشيف تاريخي مصنّف (صور، وثائق، ملفات PDF قابلة للمعاينة داخل الصفحة)، مع فترة زمنية ومصدر ووسوم وإعدادات SEO.

### announcements (الإعلانات) — 1.0.0
إعلانات وتنبيهات رسمية بعدة أنواع وأماكن عرض: بطاقة الصفحة الرئيسية، شريط علوي عبر كل صفحات الموقع (قابل للإغلاق ويبقى مغلقًا للزائر)، ونافذة منبثقة (بترددات: مرة واحدة/كل زيارة/كل عدد أيام)، مع التحكم بالظهور على الجوال والكمبيوتر بشكل مستقل لكل إعلان.

---

جميع الإضافات أعلاه اختُبرت مجتمعة عبر تثبيت كامل واحد على قاعدة بيانات MariaDB حقيقية، بما يشمل تركيبها معًا دون تعارض في المسارات أو أسماء الجداول، وتعطيل/تفعيل كل إضافة دون التأثير على البقية.

## المعروف من القيود الحالية (Known Gaps)
- إرسال فعلي عبر SMS/واتساب لم يُنفَّذ بعد (مؤجَّل عمدًا لإضافات تكامل مستقبلية مع مزود رسمي، حسب طلب المواصفات).
- رفع تحديث للنواة بصيغة ZIP من لوحة التحكم غير متاح بعد (مذكور في المواصفات كميزة مستقبلية؛ رقم الإصدار جاهز لدعمها لاحقًا).
